import LocalizedStrings from 'react-native-localization';
import en from 'localization/en';
import vn from 'localization/vn';

const strings = new LocalizedStrings({'en' : en , 'vn': vn});

export default strings;
